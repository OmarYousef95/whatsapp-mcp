# build output
dist/
node_modules/

# never commit WhatsApp session credentials, even by accident.
# (the server stores them in ~/.whatsapp-mcp/ outside the repo, but belt-and-suspenders)
auth/
*.session
.whatsapp-mcp/

# local Claude Code project instructions (not for public repo)
CLAUDE.md

# misc
.DS_Store
*.log
